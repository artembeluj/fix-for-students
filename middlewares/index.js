const validateBody = require("./validateBody");
const authenticate = require("./authenticate");
const authentificate = require("./auntificate");

module.exports = {
    validateBody,
    authenticate,
    authentificate
}